/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "miftztgfemnglhjovkgg.supabase.co",
      "salonlfc.com",
      "via.placeholder.com",
    ],
  },
};

export default nextConfig;
